<h1>All Pretty</h1>
<table border='2'>
	<tr>
		<td>pretty id</td>
		<td>name</td>
		<td>prettyStyle</td>
		<td>strength</td>
		<td>city</td>
		<% if @username=="admin" %>
		<td>edit / delete</td>
		<% end %>
	</tr>
	<% @pretties.each do |t| %>
		<tr>
			<td><%= t.id %></td>
			<td><%= t.name %></td>
			<td><%= t.prettyStyle %></td>
			<td><%= t.strength %></td>
			<td><%= t.city %></td>
			<% if @username=="admin" %>
				<td><%= link_to 'edit' , :action => 'modify', :id => t.id %> / <%= link_to 'delete' , :action => 'delete', :id => t.id %></td>
			<% end %>
		</tr>
	<% end %>
</table>
<% if @username=="admin" %>
	<p><%= link_to 'Create new pretty', 'pretty/newpretty' %></p>
<% end %>
<p><%= link_to 'Back', 'admin' %></p>