<h1>Bento</h1>
<table border='2'>
	<tr>
		<td>item name</td>
		<td>price</td>
		<td>bentoshop_id</td>
		<td>edit / delete</td>
	</tr>
	<% @bentos.each do |t| %>
		<tr>
			<td><%= t.item_name %></td>
			<td><%= t.price %></td>
			<td><%= t.bentoshop_id %></td>
			<td><%= link_to 'edit' , :action => 'modify', :id => t.id %> / <%= link_to 'delete' , :action => 'delete', :id => t.id %></td>
		</tr>
	<% end %>
</table>
<p><%= link_to 'Create new bento item', 'bento/newbento' %></p>
<p><%= link_to 'Back', 'admin' %></p>